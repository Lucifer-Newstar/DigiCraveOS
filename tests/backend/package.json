{
  "name": "digicraveos-backend-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Jest + Supertest integration tests for the POS API",
  "scripts": {
    "test": "jest --runInBand --forceExit"
  },
  "jest": {
    "testEnvironment": "node",
    "setupFilesAfterEnv": ["<rootDir>/setup.js"],
    "testTimeout": 30000
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^7.0.0"
  }
}
