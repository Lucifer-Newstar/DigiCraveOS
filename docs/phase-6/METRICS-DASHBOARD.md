# Metrics Dashboard Card

The admin Reports page now displays process metrics from `/api/health/metrics`: uptime, heap usage, database readiness, and Node version. The query refreshes every 30 seconds and remains read-only.

Frontend ESLint and production build verification passed.
