# Dependency Audit — 2026-09-22

Production dependency audits were run with `npm audit --omit=dev --json` in:

- `Restaurant_POS_System/pos-backend`: 0 vulnerabilities
- `Restaurant_POS_System/pos-frontend`: 0 vulnerabilities
- `tests/backend`: 0 vulnerabilities

No `npm audit fix --force` command was used. Existing install-time warnings remain separate from the production audit result.
